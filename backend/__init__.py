"""This submodule is the backend or server part of the protocol.
   It receives low resolution images batchwise, runs detection on them
   and asks clients to send high resolution images if needed"""
